# Telegram Summary Bot

Базовый проект LangGraph для автоматического создания сводок сообщений из Telegram чата "BitKogan / Development".

## Возможности

- Автоматический запуск каждые 5 минут
- Получение сообщений за последние 10 минут из указанной группы Telegram
- Генерация сводки с помощью модели Qwen3-Coder-Plus
- Использование OAuth креденшиалов Qwen из файла `/home/vyt/.qwen/oauth_creds.json`
- Интеграция с telegram-mcp для доступа к сообщениям (в настоящее время используется mock-клиент)

## Установка

1. Установите зависимости с помощью uv:
```bash
uv sync
```

2. Убедитесь, что у вас есть:
   - Рабочие OAuth креденшиалы Qwen в `/home/vyt/.qwen/oauth_creds.json`
   - Настроенный telegram-mcp сервер в `/home/vyt/devel/telegram-mcp` (опционально)

## Использование

### Запуск основного бота (каждые 5 минут):
```bash
uv run python -m src.main
```

### Тестирование (однократный запуск):
```bash
uv run python test_bot.py
```

### Тестирование только Qwen API:
```bash
uv run python test_qwen_only.py
```

## Архитектура

Проект использует LangGraph для создания workflow с двумя основными узлами:

1. **fetch_messages_node**: Получает сообщения из Telegram через MCP (или mock-клиент)
2. **summarize_messages_node**: Генерирует сводку с помощью Qwen

### Компоненты

- `qwen_client.py`: Клиент для Qwen API с OAuth аутентификацией
- `qwen_langchain.py`: LangChain интеграция для Qwen
- `telegram_mcp.py`: Клиент для взаимодействия с telegram-mcp (в разработке)
- `telegram_mock.py`: Mock-клиент для тестирования без реального подключения к Telegram
- `workflow.py`: LangGraph workflow для обработки сообщений
- `main.py`: Основной скрипт с планировщиком

## Конфигурация

По умолчанию бот:
- Проверяет группу "BitKogan / Development"
- Анализирует сообщения за последние 10 минут
- Запускается каждые 5 минут
- Использует модель "qwen3-coder-plus"

Эти параметры можно изменить в соответствующих файлах.

## Текущее состояние

✅ **Работает:**
- Qwen API интеграция с OAuth токенами
- LangGraph workflow
- Генерация сводок
- Mock-клиент для тестирования

🔄 **В разработке:**
- Интеграция с реальным telegram-mcp (проблемы с подключением)

## Требования

- Python 3.11+
- Рабочие OAuth креденшиалы Qwen
- uv для управления зависимостями
- Доступ к telegram-mcp серверу (опционально)
